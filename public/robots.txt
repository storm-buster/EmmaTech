# EmmaTech / RAPHA — robots policy
# The public site uses real pathname routes (e.g. /rapha, /compliance, /docs/*),
# each crawlable with its own metadata; see the sitemap below. Private/app routes
# (login, signup, account, deploy, console, request-access) are served noindex via
# X-Robots-Tag and are intentionally excluded from the sitemap.
User-agent: *
Allow: /
# Application endpoints are not content — keep them out of the index.
Disallow: /api/

Sitemap: https://www.emmatech.in/sitemap.xml
